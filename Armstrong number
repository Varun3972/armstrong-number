#include <stdio.h>

int main() { int n, sum=0,r emp, c; printf("Enter a number: ");

scanf("%d",&n);

temp=n;

while(n>0)

{

the -

}

r=n%10;

c=r*r*r;

sum = sum + c;

n=n/10;
}

if(sum==temp)

printf("%d is a Armstrong number", temp); 

else

printf("%d is not a Armstrong number", temp);

return 0;

}
